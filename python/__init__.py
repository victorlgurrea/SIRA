"""Módulos Python de SIRA.

Puntos de entrada en esta carpeta:
  api_server.py        → sira.api.server (uvicorn)
  ingesta.py           → python -m sira.services.ingesta
  scheduler.py         → python -m sira.services.ingesta --scheduler
  bootstrap_ingesta.py → python -m sira.services.ingesta --bootstrap

Scripts utilitarios: ``scripts/`` (build, research, migración).
"""
